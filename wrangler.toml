# Cloudflare Pages 部署配置
name = "ai-detective-game"
compatibility_date = "2024-11-01"
pages_build_output_dir = "public"

# 环境变量（生产）—— 注意：LM_BASE_URL 用 secret 管理（动态变化，避免 binding 冲突）
[vars]
LM_MODEL = "qwen/qwen3.6-35b-a3b"
